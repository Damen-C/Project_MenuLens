name: Android Quality

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  android-ci:
    runs-on: ubuntu-latest
    env:
      GRADLE_OPTS: -Xmx4g -XX:MaxMetaspaceSize=1g -Dorg.gradle.daemon=false
    defaults:
      run:
        working-directory: android
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Restore Firebase config
        env:
          GOOGLE_SERVICES_JSON_B64: ${{ secrets.GOOGLE_SERVICES_JSON_B64 }}
        run: |
          if [ -z "$GOOGLE_SERVICES_JSON_B64" ]; then
            echo "Missing GOOGLE_SERVICES_JSON_B64 GitHub secret"
            exit 1
          fi
          echo "$GOOGLE_SERVICES_JSON_B64" | base64 --decode > app/google-services.json

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Lint debug
        run: ./gradlew lintDebug --stacktrace --no-daemon --max-workers=2

      - name: Run debug unit tests
        run: ./gradlew testDebugUnitTest --stacktrace --no-daemon --max-workers=2

      - name: Assemble debug APK
        run: ./gradlew assembleDebug --stacktrace --no-daemon --max-workers=2
