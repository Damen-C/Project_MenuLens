# Work Log - 2026-03-16

## Summary
- Deployed the Firebase-auth + quota-gating backend image to Cloud Run and verified the new `/docs` schema was live.
- Validated backend monthly gating behavior with temporary `FREE_SCAN_LIMIT_PER_MONTH=2` test configuration.
- Added a safe developer bypass allowlist keyed by Firebase UID.
- Fixed Android results screen clipping by making the list scroll and respect bottom navigation-bar insets.

## Detailed Work Completed
1. Cloud Run rollout verification
- Switched Cloud Run from env-only revision changes to a new container image built from `feat/firebase-auth-quota-foundation`.
- Confirmed the backend API schema now exposes `request_id` in `POST /v1/scan_menu`, proving the new code is live.

2. Gating validation
- Temporarily set:
  - `ENABLE_FIREBASE_AUTH=false`
  - `FREE_SCAN_LIMIT_PER_MONTH=2`
- Verified behavior in Swagger UI:
  - request 1 => `200`
  - request 2 => `200`
  - request 3 => `402` with `scan_quota_exceeded`
- Reverted to normal operating plan:
  - `ENABLE_FIREBASE_AUTH=true`
  - `FREE_SCAN_LIMIT_PER_MONTH=10`
  - `PRO_SCAN_LIMIT_PER_MONTH=250`

3. Developer bypass implementation
- Added `DEV_BYPASS_QUOTA_UIDS` backend env var.
- Backend now bypasses quota only for allowlisted Firebase UIDs.
- Bypass is server-side only and does not rely on client debug flags.
- Allowlisted users return usage plan `dev_unlimited` for diagnostics.

4. Android UI fix for long result lists
- Replaced non-scrollable results `Column` with `LazyColumn`.
- Added bottom inset-aware padding using navigation-bar window insets.
- Verified the last results card is no longer clipped behind the Android system navigation bar.

5. Validation performed
- Backend compile check passed:
  - `python -m compileall app/main.py app/usage.py`
- Android tests passed:
  - `./gradlew.bat testDebugUnitTest`

## Current End-of-Day State
- This branch now has:
  - Firebase anonymous auth app wiring
  - backend-enforced monthly scan gating
  - request idempotency
  - safe developer UID bypass
  - fixed scroll behavior for long results screens
- Cloud Run is running the updated backend image.

## Next Recommended Steps
1. Add an app UI surface for quota awareness:
- show `Free: 10 scans/month`
- show current usage/remaining count
- show friendly limit-reached state instead of generic backend error
2. Implement Google Play Billing and backend purchase verification.
3. Replace SQLite `/tmp/scan_usage.db` with a shared production datastore (Cloud SQL or Firestore) so quota counting is correct across multiple Cloud Run instances.
4. Commit and push today’s allowlist + results-screen fix if you want these changes available in GitHub immediately.