# Work Log - 2026-05-06

## Summary

- Created `menu_parse_v2` to improve exact Japanese item-name extraction.
- Ran evals against the existing 8-example menu fixture dataset.
- Adopted `menu_parse_v2` as the default backend prompt version.
- Updated project documentation to reflect the current final state.
- Marked the project as effectively complete.

## Detailed Work Completed

1. Prompt iteration
- Added `backend/app/prompts/menu_parse_v2.txt`.
- Tightened extraction rules to reduce generic fragments, menu add-ons, set components, and marketing copy in `jp_text`.
- Kept `menu_parse_v1` available for comparison through prompt version selection.

2. Evaluation run
- Ran `python -m evals.run_evals` with `MENU_PARSE_PROMPT_VERSION=menu_parse_v2`.
- Generated report:
  - `backend/evals/results/eval_report_20260506T013708Z.json`
- Confirmed the CSV ledger records prompt version and summary metrics:
  - `backend/evals/results/eval_runs.csv`

3. Eval outcome
- `menu_parse_v2` matched the prior recall while improving precision and lowering hallucinated items.
- Baseline comparison:
  - `item_recall`: `0.8708` -> `0.8708`
  - `item_precision_proxy`: `0.7704` -> `0.8430`
  - `hallucinated_item_rate`: `0.2296` -> `0.1570`
  - `mean_latency_ms`: `20646.6` -> `20041.9`
  - `p95_latency_ms`: `51410.5` -> `45657.8`
- Notable behavioral improvement:
  - `menu-5` no longer extracted the refill text as a standalone item.
- Remaining known imperfections:
  - `menu-1` still over-extracts some side/add-on content.
  - `menu-4` can still produce an overly generic item.
  - `menu-6` can still under-specify one handwritten kakiage item.

4. Default app behavior
- Updated `backend/app/prompts/registry.py` so the default `menu_parse` version is now `menu_parse_v2`.
- Verified the backend resolves `menu_parse` to `menu_parse_v2` without requiring an env override.

5. Documentation refresh
- Rewrote the root `README.md` as a concise final-state summary of the project.
- Updated README eval guidance to show how to run evals locally and how to inspect prompt-versioned results.

## Validation Performed

- Confirmed `menu_parse_v2` prompt file loads through the prompt registry.
- Ran the live eval suite successfully with no failures.
- Confirmed the generated JSON report includes `menu_parse_prompt_version`.
- Confirmed the CSV ledger includes the `menu_parse_prompt_version` column and the `menu_parse_v2` run row.
- Confirmed the default prompt resolution returns `menu_parse_v2`.

## Current End-of-Day State

- Active default prompt versions:
  - `menu_parse_v2`
  - `ocr_normalize_v1`
- Latest eval report:
  - `backend/evals/results/eval_report_20260506T013708Z.json`
- Project status:
  - Functionally complete
  - Suitable for demo, portfolio, and case-study use
  - Future work is optional polish rather than required delivery work

## Recommended Follow-Up

If work resumes later, the highest-value optional follow-ups are:

1. Add a baseline comparison helper script for faster eval review.
2. Add CI regression gating for prompt and model changes.
3. Reduce the remaining over-extraction and under-specific handwritten cases.
4. Replace SQLite quota storage if the backend is ever deployed as a real multi-instance service.
